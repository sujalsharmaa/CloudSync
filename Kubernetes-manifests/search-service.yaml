apiVersion: v1
kind: Service
metadata:
  name: cloudsync-upload-service-Service
spec:
  selector:
    app: cloudsync-upload-service
  ports:
    - protocol: TCP
      port: 8085
      targetPort: 8085
  type: LoadBalancer
  sessionAffinity: ClientIP